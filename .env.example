# PPT Master 环境配置

## 快速开始

复制本文件为 `.env` 并修改：

```bash
cp .env.example .env
```

## 图片生成后端

`image_gen.py` 通过 `IMAGE_BACKEND` 环境变量选择后端。
所有后端都通过 `.env` 文件配置，无需修改代码。

### 推荐后端

| 场景 | IMAGE_BACKEND | API Key | Base URL (可选) |
|------|---------------|---------|-----------------|
| 免费API（推荐） | **openai** | `OPENAI_API_KEY=你的key` | `OPENAI_BASE_URL=https://api.agnes-ai.com` |
| Google | **gemini** | `GEMINI_API_KEY=你的key` | — |
| OpenAI官方 | **openai** | `OPENAI_API_KEY=你的key` | 默认 |
| 七牛/硅基 | **openai** | `OPENAI_API_KEY=你的key` | `OPENAI_BASE_URL=https://api.siliconflow.cn` |
| 阿里百炼 | **qwen** | `QWEN_API_KEY=你的key` | — |
| MiniMax | **minimax** | `MINIMAX_API_KEY=你的key` | — |
| 智谱 | **zhipu** | `ZHIPU_API_KEY=你的key` | — |

### 示例：配置免费 Agnes API

```bash
IMAGE_BACKEND=openai
OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.agnes-ai.com
# 图片模型
OPENAI_IMAGE_MODEL=agnes-image-2.1-flash
# 视频模型（可选）
OPENAI_VIDEO_MODEL=agnes-video-2.0
```

### 示例：配置 OpenAI 官方

```bash
IMAGE_BACKEND=openai
OPENAI_API_KEY=sk-your-openai-key
# 使用默认 base url
OPENAI_IMAGE_MODEL=dall-e-3
```

### 支持的模型参数

| 参数 | 环境变量 | 默认值 |
|------|---------|-------|
| 图片模型 | `OPENAI_IMAGE_MODEL` | `dall-e-3` |
| 视频模型 | `OPENAI_VIDEO_MODEL` | — |
| 分辨率 | `IMAGE_DEFAULT_SIZE` | `1024x1024` |

## 更多后端

全部支持列表：运行 `python3 scripts/image_gen.py --list-backends`

- gemini, openai, minimax, stability, bfl (FLUX), ideogram
- qwen, zhipu, volcengine, modelscope, siliconflow
- fal, replicate, openrouter
